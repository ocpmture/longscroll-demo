Demo of how forum thread views should look like: continuous, unlimited scrolling while still being able to go to a specific post number.

The jquery script is at public/javascripts/jquery.bidirectionalscroller.js. It depends on a few server side functions and so is not easily pluggable to any application yet.
